/**
 * Map pour stocker l'ordre initial des éléments dans chaque dropdown
 * @type {Map<string, number>}
 * La clé est une chaîne de caractères combinant le texte de l'élément et l'ID du conteneur
 * la valeur est un index représentant la position de l'élément dans l'ordre initial
 */
export const initialOrder = new Map();
